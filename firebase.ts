/**
 * Punto de entrada para inicializar Firebase cuando se disponga de las credenciales.
 *
 * La aplicación no depende de Firebase directamente, pero este archivo se mantiene como
 * referencia para quienes deseen agregar Analytics, Authentication u otros servicios.
 * Instala el SDK oficial (`npm install firebase`) y reemplaza el contenido por la
 * inicialización real:
 *
 * import { initializeApp } from 'firebase/app';
 * const firebaseConfig = { ... };
 * export const app = initializeApp(firebaseConfig);
 */
export {}; // Archivo marcador sin implementación por defecto.
